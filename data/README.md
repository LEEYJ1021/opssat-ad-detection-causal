# Data

This directory does not contain the raw OPS-SAT-AD dataset (license terms do not permit redistribution).

Run:
```bash
bash download_opssat_ad.sh
```
to populate `data/raw/` with the original dataset in the layout the pipeline expects.

Please cite the OPS-SAT-AD dataset release directly in addition to this repository's paper.
